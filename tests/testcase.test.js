describe(`Error should be parsable in afterTest`, function () {
    it(`should error out`, function () {
        browser
            .url(`/`)
            
        $(`.DoesNotExist`).waitForExist(500)
    })
})
